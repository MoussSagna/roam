# Onboarding images

## TEMPORARY — welcome screen collage

`welcome-street.png`, `welcome-terrace.png`, `welcome-lake.png` and `welcome-cafe.png` are **low-resolution
crops taken from the design mockup** (un-rotated, white frame removed). The original photos were not
available when the screen was built.

Replace them with the original photos, **keeping the file names** (any resolution ≥ 800 px on the long
side is fine; the screen crops with `cover`):

| File                  | Content                         | Shown as                                                    |
| --------------------- | ------------------------------- | ----------------------------------------------------------- |
| `welcome-street.png`  | Old-town street at sunset       | Large card, left, tilted counter-clockwise                  |
| `welcome-terrace.png` | Café terrace under trees        | Card, top right, cut by the screen edge                     |
| `welcome-lake.png`    | Lake and mountains, golden hour | Landscape card, bottom right                                |
| `welcome-cafe.png`    | Café terrace with greenery      | Card cut by the left screen edge (only a sliver is visible) |

## TEMPORARY — "Prêt à explorer ?" background

`ready-background.jpg` is a **low-resolution crop of the design mockup** (Paris rooftops at sunset, person seen from
behind). The title, subtitle, handwritten line, "Commencer" button and status bar drawn on the mockup were erased
(interpolated), so the sky and the jacket are reconstructed and the area behind the button is smeared on purpose:
the screen draws all of them itself.

Replace it with the original photo, **keeping the file name** (portrait, ≥ 1200 px wide; the screen fills the
whole display with `cover`, anchored at the bottom).

## TEMPORARY — profile creation landscape

`profile-landscape.jpg` (Alpine valley at sunset with a hiker) is a **crop of the design mockup**, upscaled 2×. The handwritten
line and the small bar drawn over it on the mockup were erased (interpolated), which leaves a soft haze on the left mountain;
the screen redraws both. Replace it with the original illustration, **keeping the file name** (any width ≥ 1000 px, about 2:1
landscape). The screen fades its top edge into the page background and anchors it at the bottom.
